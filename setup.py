from setuptools import setup, find_packages

setup(
    name='bprop',
    version='0.1',
    description='Reinvent back propagation for common operations to get a better understanding.',
    author='Wil Kong',
    packages=find_packages(),
    test_suite="tests",
)
